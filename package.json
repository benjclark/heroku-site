{
  "name": "heroku-node",
  "description": "Sample Node to Heroku app",
  "main": "server.js",
  "scripts": {
      "build": "node-sass styles.scss public/css/styles.css",
      "start": "npm i && npm run build && node server.js"
  },
  "dependencies": {
    "express": "~4.9.8"
  }
}
